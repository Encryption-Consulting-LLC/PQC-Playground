# --- Python (backend/) ---
__pycache__/
*.pyc
.venv/
*.egg-info/
*.iso

# pki-executor agent build artifact — a per-host binary pointed at by
# EXECUTOR_AGENT_PATH, not vendored in the repo.
backend/agent/pki-executor.exe

# --- Node / Vite (frontend/) ---
node_modules/
dist/
dist-ssr/
*.local

# --- Env / editor ---
.env
.env.*
!.env.example
.DS_Store
*.log
